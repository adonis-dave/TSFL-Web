# Database
DB_USER=sfl
DB_PASSWORD=sfl_password
DB_NAME=sfl_tanzania
DATABASE_URL=postgres://${DB_USER}:${DB_PASSWORD}@localhost:5433/${DB_NAME}

# Backend
PORT=4000
CORS_ORIGIN=*

# JWT
JWT_SECRET=change-me-to-a-random-64-char-hex-string

# Frontend
NEXT_PUBLIC_API_URL=/api

# Admin (first-run credentials — change after login)
ADMIN_EMAIL=admin@sfltanzania.org
ADMIN_PASSWORD=admin123
